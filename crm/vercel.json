{
  "framework": "nextjs",
  "buildCommand": "npm run build",
  "outputDirectory": ".next",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- . || exit 1",
  "crons": [
    {
      "path": "/api/v1/cron/elevenlabs",
      "schedule": "*/5 * * * *"
    }
  ]
}
